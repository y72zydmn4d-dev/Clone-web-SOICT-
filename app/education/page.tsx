"use client";
import { useMemo, useState } from "react";
import { courses } from "@/data/courses"; import type { Course } from "@/data/types"; import { CourseCard, CourseModal, EmptyState, PageHero } from "@/components/ui";
export default function Education(){ const [tab,setTab]=useState<Course["degree"]>("Undergraduate"); const [query,setQuery]=useState(""); const [selected,setSelected]=useState<Course|null>(null); const filtered=useMemo(()=>courses.filter(c=>c.degree===tab && c.name.toLowerCase().includes(query.toLowerCase())),[tab,query]); return <><PageHero eyebrow="Education" title="A strong foundation for a changing world." text="Programs that give students depth, flexibility and the confidence to keep learning."/><section className="container-site section-space"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div className="flex flex-wrap gap-2">{(["Undergraduate","Graduate","PhD"] as Course["degree"][]).map(x=><button onClick={()=>setTab(x)} key={x} className={`rounded-full px-4 py-2 text-sm font-bold ${tab===x?"bg-brand text-white":"surface"}`}>{x}</button>)}</div><input aria-label="Search programs" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search programs" className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm outline-none focus:border-brand"/></div><div className="mt-8 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map(x=><CourseCard key={x.id} item={x} onOpen={setSelected}/>)}</div>{!filtered.length&&<div className="mt-8"><EmptyState text="No programs match your search."/></div>}</section><CourseModal course={selected} onClose={()=>setSelected(null)}/></> }
