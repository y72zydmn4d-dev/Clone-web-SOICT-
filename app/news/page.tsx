"use client";
import { useMemo, useState } from "react"; import { news } from "@/data/news"; import { EmptyState, NewsCard, PageHero } from "@/components/ui";
const categories=["All",...Array.from(new Set(news.map(x=>x.category)))];
export default function News(){const [q,setQ]=useState("");const [category,setCategory]=useState("All");const [sort,setSort]=useState("newest");const [page,setPage]=useState(1);const perPage=6;const filtered=useMemo(()=>news.filter(x=>(category==="All"||x.category===category)&&`${x.title} ${x.excerpt}`.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>sort==="newest"?b.date.localeCompare(a.date):a.date.localeCompare(b.date)),[q,category,sort]);const pages=Math.ceil(filtered.length/perPage);const items=filtered.slice((page-1)*perPage,page*perPage);return <><PageHero eyebrow="News & stories" title="What’s happening at SOICT." text="Updates from our research, classrooms, students and community."/><section className="container-site section-space"><div className="surface mb-8 grid gap-3 rounded-2xl p-4 md:grid-cols-[1fr_auto_auto]"><input value={q} onChange={e=>{setQ(e.target.value);setPage(1)}} placeholder="Search news" className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm outline-none focus:border-brand"/><select value={category} onChange={e=>{setCategory(e.target.value);setPage(1)}} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm">{categories.map(x=><option key={x}>{x}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm"><option value="newest">Newest first</option><option value="oldest">Oldest first</option></select></div>{items.length?<><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{items.map(x=><NewsCard item={x} key={x.id}/>)}</div><div className="mt-10 flex justify-center gap-2">{Array.from({length:pages},(_,i)=><button onClick={()=>setPage(i+1)} key={i} className={`grid h-9 w-9 place-items-center rounded-lg text-sm font-bold ${page===i+1?"bg-brand text-white":"surface"}`}>{i+1}</button>)}</div></>:<EmptyState/>}</section></>}
