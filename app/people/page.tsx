"use client";
import { useMemo, useState } from "react"; import { people } from "@/data/people"; import { EmptyState, PageHero, PersonCard } from "@/components/ui";
const departments=["All",...Array.from(new Set(people.map(x=>x.department)))]; const areas=["All",...Array.from(new Set(people.flatMap(x=>x.interests)))];
export default function People(){const [q,setQ]=useState("");const [dept,setDept]=useState("All");const [area,setArea]=useState("All");const list=useMemo(()=>people.filter(x=>(dept==="All"||x.department===dept)&&(area==="All"||x.interests.includes(area))&&`${x.name} ${x.position}`.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>a.name.localeCompare(b.name)),[q,dept,area]);return <><PageHero eyebrow="People" title="People make the difference." text="Meet the faculty and academic leaders who guide our learning and research community."/><section className="container-site section-space"><div className="surface mb-8 grid gap-3 rounded-2xl p-4 lg:grid-cols-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search people" className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm outline-none focus:border-brand"/><select value={dept} onChange={e=>setDept(e.target.value)} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm">{departments.map(x=><option key={x}>{x}</option>)}</select><select value={area} onChange={e=>setArea(e.target.value)} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm">{areas.map(x=><option key={x}>{x}</option>)}</select></div>{list.length?<div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{list.map(x=><PersonCard key={x.id} item={x}/>)}</div>:<EmptyState text="No people match these filters."/>}</section></>}
