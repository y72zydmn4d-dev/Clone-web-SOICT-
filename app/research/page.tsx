"use client";
import { useState } from "react"; import { research } from "@/data/research"; import { PageHero, ResearchCard } from "@/components/ui";
export default function Research(){const [active,setActive]=useState<number|null>(null);const item=research.find(x=>x.id===active);return <><PageHero eyebrow="Research" title="Research that meets the moment." text="From foundations to applications, our research groups create knowledge and technology with lasting value."/><section className="container-site section-space"><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{research.map(x=><button onClick={()=>setActive(x.id)} className="text-left" key={x.id}><ResearchCard item={x}/></button>)}</div>{item&&<section className="surface mt-8 rounded-2xl p-7 fade-in"><button onClick={()=>setActive(null)} className="float-right text-sm font-bold text-brand">Close</button><p className="eyebrow">Research focus</p><h2 className="mt-2 text-3xl font-bold">{item.title}</h2><p className="muted mt-4 max-w-3xl leading-7">{item.description}</p><div className="mt-7 grid gap-6 sm:grid-cols-2"><div><h3 className="font-bold">Related faculty</h3><ul className="mt-3 space-y-2 text-sm">{item.faculty.map(x=><li key={x}>{x}</li>)}</ul></div><div><h3 className="font-bold">Featured projects</h3><ul className="mt-3 space-y-2 text-sm">{item.projects.map(x=><li key={x}>{x}</li>)}</ul></div></div></section>}</section></>}
