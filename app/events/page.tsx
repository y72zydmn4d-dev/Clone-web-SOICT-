"use client";
import { useMemo, useState } from "react"; import { events } from "@/data/events"; import { EmptyState, EventCard, PageHero } from "@/components/ui";
const categories=["All",...Array.from(new Set(events.map(x=>x.category)))];
export default function Events(){const [q,setQ]=useState("");const [cat,setCat]=useState("All");const [range,setRange]=useState("upcoming"); const listed=useMemo(()=>events.filter(x=>(cat==="All"||x.category===cat)&&x.title.toLowerCase().includes(q.toLowerCase())&&(range==="all"||range==="upcoming"?x.date>="2026-09-08":x.date<"2026-09-08")).sort((a,b)=>a.date.localeCompare(b.date)),[q,cat,range]);return <><PageHero eyebrow="Events" title="Gather around big ideas." text="Seminars, opportunities and moments for the whole SOICT community."/><section className="container-site section-space"><div className="surface mb-8 grid gap-3 rounded-2xl p-4 md:grid-cols-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search events" className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm outline-none focus:border-brand"/><select value={cat} onChange={e=>setCat(e.target.value)} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm">{categories.map(x=><option key={x}>{x}</option>)}</select><select value={range} onChange={e=>setRange(e.target.value)} className="rounded-xl border border-slate-200 px-4 py-2.5 text-sm"><option value="upcoming">Upcoming events</option><option value="past">Past events</option><option value="all">All events</option></select></div>{listed.length?<div className="grid gap-5 md:grid-cols-2 lg:grid-cols-3">{listed.map(x=><EventCard key={x.id} item={x}/>)}</div>:<EmptyState text="No events match these filters."/>}</section></>}
